---
title: Identity Operators
description: Check if two variables refer to the same object.
icon: Equal
---

Identity operators check whether two variables point to the same object in
memory.

<Callout type="info">
Use `is` for identity checks (like `x is None`), and `==` for value equality.
</Callout>

## Operators
| Operator | Meaning | Example |
| --- | --- | --- |
| `is` | same object | `x is y` |
| `is not` | different objects | `x is not y` |

## Example
```python
x1 = 5
y1 = 5
x2 = "Hello"
y2 = "Hello"
x3 = [1, 2, 3]
y3 = [1, 2, 3]

print(x1 is y1)   # True (small ints may be cached)
print(x2 is y2)   # True (strings may be interned)
print(x3 is y3)   # False (different list objects)
print(x3 == y3)   # True (same contents)
```

## Rules

1. **`is` compares object identity, `==` compares value.** Two equal objects can be distinct objects.
2. **Use `is` only with `None`, `True`, and `False`.** `if x is None:` is the correct idiom.
3. **Never use `is` to compare numbers or strings.** Small integers and short strings are cached, so `is` may appear to work and then silently stop working on larger values.
4. **`is not` is a single operator.** Write `x is not None`, not `not x is None`.
5. **Identity is what `id()` reports.** `a is b` is exactly `id(a) == id(b)`.

## Common Mistakes
- Using `is` to compare values instead of identity.
- Assuming `is` behaves like `==` for strings/lists.
- Relying on interning or caching behavior.


## Quick Quiz
1. What does `is` check?
2. When should you use `x is None`?
3. Why can `is` be misleading for lists?

## Keep going

<Cards>
  <Card title="Membership Operators" href="/docs/week2/operators-membership" description="Next in the operator pages." />
  <Card title="Comparison Operators" href="/docs/week2/operators-comparison" description="The == that 'is' is often confused with." />
  <Card title="Operators" href="/docs/week2/operators" description="Back to the overview." />
</Cards>
