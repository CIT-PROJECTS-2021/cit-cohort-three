---
title: Functions
description: Learn what functions are, how to define them, and how return values work.
---

# Functions

Functions group reusable logic into a named block.

<Callout type="info">
Think of a function as a mini‑program: inputs in, output out.
</Callout>

## Defining a Function
```python
def greet(name: str) -> None:
    """Print a friendly greeting."""
    print(f"Hello, {name}!")
```

**Parameters vs arguments**
- Parameters are the names in the function definition.
- Arguments are the values you pass when calling the function.

**Type hints (optional)**
```python
def add(a: int, b: int) -> int:
    return a + b
```

## Calling a Function
```python
greet("Paul")
```

## Return Values
```python
def absolute_value(n: int) -> int:
    if n >= 0:
        return n
    return -n

print(absolute_value(-4))  # 4
```

If you omit `return`, Python returns `None`.

**Why return values matter**
Returning values lets you reuse results in other parts of your program instead
of only printing them.

**Return vs print**
```python
def add(a, b):
    return a + b

result = add(2, 3)
print(result)  # 5
```

## Scope
```python
x = 10

def show() -> None:
    x = 5
    print(x)  # 5

show()
print(x)      # 10
```

**Global variables**
Avoid using `global` unless necessary; it makes code harder to test.

## Types of Functions
- Built-in functions (e.g., `print`, `len`)
- User-defined functions (your own)

## Mini Exercises
1. Write a function `add(a, b)` that returns the sum.
2. Write a function `is_even(n)` that returns `True` or `False`.
3. Write a function that returns the maximum of three numbers.

## Common Mistakes
- Forgetting to return a value when it’s needed.
- Calling a function before it is defined.
- Mutating global variables accidentally.

## Quick Quiz
1. What does a function return if there is no `return`?
2. What are parameters vs arguments?
3. Why prefer returning values over printing?


## Common Mistakes
- Printing instead of returning when a value is needed.
- Forgetting to call the function after defining it.
- Using global variables unnecessarily.


## Quick Quiz
1. What does a function return if there is no `return` statement?
2. What is the difference between parameters and arguments?
3. Why prefer returning a value over printing it?
